"desactiva la compativilidad con vi"
set nocompatible

"activa la sintaxios por color"
syntax on 

"activa el numero de linea"
set nu
set relativenumber

"Quita los archivos swap"
set nobackup
set nowritebackup
set noswapfile

set autoindent
set expandtab "el identado son espacios
set shiftwidth=4 "el identado es de 4 espacios

set encoding=utf-8
set hidden
set ignorecase

set splitbelow splitright
set foldmethod=manual 
